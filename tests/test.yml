# test file for fonts
---
- hosts: localhost
  connection: local
  become: true
  roles:
    - ../../
  vars:
    fonts_font_paths:
      - src: /tmp/ubuntu-font-family/
        dest: /usr/share/fonts/truetype/ubuntu-font-family

      - src: /tmp/ubuntu-font-family-0.84/
        dest: /usr/share/fonts/truetype/ubuntu-font-family-0.84
        state: absent
